const axios = require('axios')
const cheerio = require('cheerio')
const express = require('express')

async function getPriceSneaker() {
    try{
        const siteUrl = 'stockx.com/'

        const data 

    } catch(err) {
        console.log(err)
    }
}
